rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Product images: public read, admin-only write/delete
    match /products/{allPaths=**} {
      allow read: if true;
      allow write, delete: if request.auth != null;
    }
  }
}
